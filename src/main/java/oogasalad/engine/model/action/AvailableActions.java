package oogasalad.engine.model.action;

import java.util.Arrays;
import java.util.Collection;
import java.util.stream.Stream;
import oogasalad.engine.model.action.Action;
import oogasalad.engine.model.board.Board;

public class AvailableActions {

//  public Collection<Action> getPotentiallyLegalActions(Board board, Player player, Rules[] rules){
//    Action availableActions = board.getStream().
//
//  }

//    public Collection<Action> getAvailableActions(Board board, ActionTypes[] actionTypes, Player player, Rules[] rules) {
//       Action[] possibilities = getActionPossibilities(actionTypes, board, player, rules);
//       Stream<Action> avail Arrays.stream(possibilities).
//           filter(action -> {Arrays.stream(rules).
//               map(rule -> rule(action)).
//               ar(Boolean::getBoolean).toArray();
//    }

//    private Action[] getActionPossibilities(ActionTypes[] actionTypes, Board board, Player player, Rules[] rules) {
//    }

}
// available actions -> a function/mapping between a state of the board, a player, and a list of rules that returns a list of actions
